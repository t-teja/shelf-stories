/**
 * @deprecated Use `src/lib/storage` instead.
 * Thin re-exports kept for compatibility during the dual-mode refactor.
 */
export {
  getAllItems,
  putItem,
  deleteItem,
  clearAllItems,
  putAllItems,
  getMeta,
  setMeta,
} from './dbCompat';
